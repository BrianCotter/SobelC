 
## Info on images
The real C code writes and reads only from **.pgm** (portable grey-map) files.

The **.png** images are also added to easily embed in markdown and show output on the webpage. PNG was automatially generated from pgm files. 
